@using Virrum.Web.Content.resources
@using Virrum.Web.Extensions
<nav class="navbar navbar-default virrum-navbar" role="navigation">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/">
                <img src="/Content/img/favicon.png" alt="@Language.Company" />
                <span class="navbar-text pull-right logo-text">@Language.AppName</span>
            </a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                @Html.CurrentControllerLink("Brukere", "Index", "Users")
                @*@if (User.IsInRole("ATC-SystemAdministrator") || User.IsInRole("ATC-PracticeTrackAdministrator") || User.IsInRole("ATC-SuperVisor"))
                {
                    @Html.CurrentControllerLink("Baner", "Index", "PracticeTracks")
                }
                @Html.CurrentControllerLink("Trafikkskoler", "Index", "Drivingschool")
                @{
                    if (User.IsInRole("ATC-SystemAdministrator"))
                    {
                        @Html.CurrentControllerLink("Brukere", "Index", "UsersDto")
                        @Html.CurrentControllerLink("Admin", "Index", "Admin")
                    }
                }*@
                
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li><a href="#">@User.Identity.Name</a></li>
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>